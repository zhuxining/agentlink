pre-commit:
  parallel: true
  jobs:
    - name: biome
      glob: "*.{js,ts,jsx,tsx,json,jsonc,css}"
      run: bun run fix
      stage_fixed: true
    - name: check-types
      run: bun run check-types
      stage_fixed: true