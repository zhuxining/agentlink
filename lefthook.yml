pre-commit:
  parallel: true
  jobs:
    - name: biome
      glob: "*.{js,ts,jsx,tsx,json,jsonc,css}"
      run: bun run fix
      stage_fixed: true
    - name: type-check
      glob: "*.{js,ts,jsx,tsx,json,jsonc,css}"
      run: bun run type-check
      stage_fixed: true