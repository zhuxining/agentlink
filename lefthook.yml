pre-commit:
  parallel: true
  jobs:
    - name: check
      glob: "*.{js,ts,jsx,tsx,json,jsonc,css}"
      run: bun run check
      stage_fixed: true

    - name: check-types
      run: bun run check-types
      stage_fixed: true