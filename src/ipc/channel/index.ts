import {
  disableAdapter,
  enableAdapter,
  listAdapters,
  listEnabledAdapters,
} from "./handlers";

export const channel = {
  disableAdapter,
  enableAdapter,
  listAdapters,
  listEnabledAdapters,
};
